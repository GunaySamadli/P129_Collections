using ConsoleApp3.Models;
using System;

namespace ConsoleApp3
{
    class Program
    {
        static void Main(string[] args)
        {
            #region 1

            //            string countstr;
            //            int count;

            //            do
            //            {
            //                Console.WriteLine("Imtahan sayini daxil edin!");
            //                countstr = Console.ReadLine();
            //            }
            //            while (!int.TryParse(countstr, out count)&&count<0);

            //            Student student = new Student();
            //            for (int i = 0; i < count; i++)
            //            {
            //                string examname;
            //                do
            //                {
            //                    Console.WriteLine($"{i+1} Fenn adini daxil edin!");
            //                    examname = Console.ReadLine();
            //                }
            //                while (student.Exams.ContainsKey(examname));

            //                string exampointstr;
            //                int exampoint;
            //                do
            //                {
            //                    Console.WriteLine($"{i+1} Fenn balini daxil edin!");
            //                    exampointstr = Console.ReadLine();
            //                }
            //                while (!int.TryParse(exampointstr, out exampoint)&& exampoint<0 && exampoint>100);

            //                student.Exams.Add(examname, exampoint);
            //            }


            //            Console.WriteLine($"Ortalama bal {student.CalcAvgPoint()}");
            #endregion



        }
    }
}
